// import { getServerAuthSession } from "@/server/auth";
// import { type generateWalletReturnType } from "onChain/wallet/wallet";
// import { getSession } from "next-auth/react";
// import React from "react";

// export const WalletProvider = ({ children }: React.ReactNode) => {
//   const [currUser, setCurrUser] = React.useState<string|null>(null);
//   async function setUser() {
//     const userId = await getSession();
//     setCurrUser(    userId?.user.id)
//     )
//   }
//   const [wallet, setWallet] = React.useState<generateWalletReturnType | null>(
//     null,
//   );
//   //get wallet

//   React.useEffect(() => {}, []);
// };
